class Tree extends Phaser.GameObjects.GameObject {
  constructor(scene) {
    super(scene);
    // this.gameScene = scene;
    // this.tile = convert x and y to tile measurements and use it to find the tile that it is on
    // this.type = 'tree';
    // this.x = x;
    // this.y = y;
    // this.scene.add.existing(this);
  }
}